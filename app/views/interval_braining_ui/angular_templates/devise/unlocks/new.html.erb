<div card-container class="col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
  <h3>Resend unlock instructions</h3>

  <%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :class => 'card', :method => :post }) do |f| %>
    <%= devise_error_messages! %>

    <div><%= f.label :email %><br />
    <%= f.email_field :email, :autofocus => true %></div>

    <div><%= f.submit 'Resend unlock instructions' %></div>
  <% end %>

  <%= render 'devise/shared/links' %>
</div>
